using System;

namespace coursework3.Classes
{
    class Auth
    {
        public DateTime AuthDate { get;  set; }

        public Auth(DateTime authDate)
        {
            AuthDate = authDate;
        }
    }
}
